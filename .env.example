# SvelteKit Public Environment Variables
# In SvelteKit, variables prefixed with PUBLIC_ are exposed to the client-side code.

# The public URL of the backend API (NestJS service)
PUBLIC_BACKEND_URL="http://localhost:3000"
